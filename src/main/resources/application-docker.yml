spring:
  datasource:
    url: jdbc:postgresql://db:5432/returnradar_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
